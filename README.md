# AI Client Brief Generator

Инструмент для автоматического превращения текстовых материалов клиентского диалога в структурированный PDF-отчет на русском языке.

Проект ориентирован на русскоязычные бизнес-сценарии и формирует PDF-отчеты с корректной кириллицей, датами и бюджетами в рублях.

Загружает текстовый файл с расшифровкой переговоров, отправляет его в OpenAI, получает структурированные данные и генерирует аккуратный деловой PDF — готовый для архива, передачи в команду или портфолио.

---

## Как проект получает данные

Проект **не подключается напрямую** к CRM, Telegram, Google Docs или другим сервисам. На текущем этапе единственный источник — локальный `.txt`-файл с транскрибацией или текстовыми материалами.

Как получить файл:

- **Zoom / Google Meet** — скачать автоматическую транскрибацию из записи встречи.
- **Telegram** — скопировать переписку в `.txt`.
- **Диктофон / телефонный звонок** — расшифровать через Whisper, Yandex SpeechKit или другой сервис, сохранить как `.txt`.
- **CRM** — экспортировать историю переписки или комментарии к сделке.

После этого положить файл в папку `input_dialogs/` и запустить генерацию.

---

## Что умеет

1. Читает текстовые материалы / транскрибацию клиентского разговора (UTF-8).
2. Отправляет текст в OpenAI и извлекает структурированную информацию: имя клиента, компанию, суть запроса, проблемы и потребности, требования, сроки, бюджет, риски и следующие шаги.
3. Валидирует JSON-ответ по строгой схеме.
4. Подставляет данные в HTML-шаблон, оптимизированный под кириллицу.
5. Генерирует PDF через WeasyPrint и сохраняет в папку `reports/`.

---

## Структура проекта

```
ai-client-brief-generator/
├── main.py                               # Точка входа (CLI)
├── example_dialogs/                      # Учебные примеры для проверки проекта
│   ├── ai_bot_client_dialog.txt          # Telegram-бот для обработки заявок
│   ├── website_design_dialog.txt         # Лендинг для студии йоги
│   ├── rag_assistant_client_dialog.txt   # RAG-ассистент по базе знаний
│   ├── customer_reviews_dialog.txt       # Автоматизация анализа отзывов
│   └── pdf_reports_dialog.txt            # Автогенерация PDF-отчетов
├── input_dialogs/                        # Сюда кладите ваши текстовые материалы / транскрибации
├── reports/                              # Готовые PDF сохраняются сюда
├── generated_images/                     # Зарезервировано для Фазы 2
├── templates/
│   ├── client_brief_template.html        # Шаблон отчета (кириллица, DejaVu Sans)
│   └── design_report_template.html       # Заготовка для Фазы 2
├── utils/
│   ├── logger.py
│   ├── validators.py
│   ├── ai_processor.py
│   ├── pdf_generator.py
│   └── image_generator.py               # Заготовка для Фазы 2
├── .env.example
├── requirements.txt
└── README.md
```

**`example_dialogs/`** — учебные примеры, входящие в состав проекта. Используются для проверки и демонстрации работы.

**`input_dialogs/`** — папка для ваших текстовых материалов / транскрибаций. Файлы из этой папки не попадают в git. Проект читает `.txt`-файлы в кодировке UTF-8.

---

## Требования

- Python 3.11+
- [API-ключ OpenAI](https://platform.openai.com/api-keys)
- Системные зависимости WeasyPrint (см. ниже)

### Системные зависимости WeasyPrint

**Windows** — установите [GTK3 Runtime](https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer/releases), затем перезапустите терминал. Альтернатива — WSL.

**macOS**:
```bash
brew install pango gdk-pixbuf libffi cairo
```

**Ubuntu / Debian**:
```bash
sudo apt install libpango-1.0-0 libpangoft2-1.0-0 libcairo2 libgdk-pixbuf2.0-0
```

> HTML-шаблон использует шрифт **DejaVu Sans** — он поставляется вместе с WeasyPrint и имеет полную поддержку кириллицы.

---

## Установка

```bash
cd ai-client-brief-generator

python -m venv .venv

# Windows:
.venv\Scripts\activate
# macOS / Linux:
source .venv/bin/activate

pip install -r requirements.txt
```

---

## Настройка

```bash
cp .env.example .env
```

Откройте `.env` и укажите ваш ключ:

```dotenv
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o        # необязательно, по умолчанию gpt-4o
LOG_LEVEL=INFO              # необязательно: DEBUG, INFO, WARNING, ERROR
```

---

## Команды запуска

### Посмотреть список доступных файлов

```bash
python main.py --list-files
```

### Интерактивный выбор файла

```bash
python main.py --interactive
```

Программа отобразит все `.txt`-файлы из `example_dialogs/` и `input_dialogs/`, предложит выбрать номер и запустит генерацию.

### Запуск с указанием файла (основная команда)

```bash
python main.py example_dialogs/ai_bot_client_dialog.txt --mode client_report
```

### Использование реальной транскрибации

```bash
python main.py input_dialogs/my_real_dialog.txt --mode client_report
```

### Обратная совместимость

`client_brief` сохранен как технический alias для `client_report` — старые команды не ломаются:

```bash
python main.py example_dialogs/ai_bot_client_dialog.txt --mode client_brief
```

---

## Пример вывода --list-files

```
Доступные материалы для обработки:

  [1] example_dialogs\ai_bot_client_dialog.txt  —  3225 символов
       --- Транскрипт созвона: автоматизация обработки клиентских заявок ---
  [2] example_dialogs\customer_reviews_dialog.txt  —  3021 символов
       --- Транскрипт созвона: автоматизация анализа клиентских отзывов ---
  [3] example_dialogs\pdf_reports_dialog.txt  —  3515 символов
       --- Транскрипт созвона: автоматизация PDF-отчетов ---
  [4] example_dialogs\rag_assistant_client_dialog.txt  —  3350 символов
       --- Транскрипт созвона: RAG-ассистент по внутренней базе знаний ---
  [5] example_dialogs\website_design_dialog.txt  —  3512 символов
       --- Транскрипт созвона: разработка лендинга для студии йоги ---
```

---

## Где найти готовый PDF

Имя файла включает дату и время с точностью до секунды:

```
reports/client_report_2026-06-06_12-00-28.pdf
```

Если запуск выполняется повторно — создается новый уникальный файл, существующие отчеты не перезаписываются.

---

## Обработка ошибок

| Ситуация | Сообщение |
|---|---|
| Не задан API-ключ | Подсказка с инструкцией по настройке `.env` |
| Файл не найден | `[ERROR] Файл не найден: <путь>` |
| Файл не в UTF-8 | `[ERROR] Не удалось прочитать файл как UTF-8` |
| Файл пустой | `[ERROR] Файл пустой: <путь>` |
| Нет .txt-файлов в папках | Подсказка о том, куда положить материалы |
| OpenAI вернул невалидный JSON | `[ERROR] OpenAI вернул некорректный или неполный ответ` |
| PDF заблокирован (PermissionError) | `[ERROR] Не удалось сохранить PDF. Возможно, файл уже открыт.` |
| PDF не сгенерировался (другая причина) | `[ERROR] PDF generation failed` |

### Устранение ошибки Permission denied на Windows

Если при генерации PDF появляется ошибка `Permission denied`, проверьте следующее:

- Убедитесь, что файл отчета не открыт в просмотрщике PDF (Adobe Acrobat, браузер и т.д.).
- Проект создает уникальные имена файлов с секундами, но открытый файл может быть заблокирован Windows.
- Закройте PDF-просмотрщик и повторите запуск — будет создан новый файл с актуальной меткой времени.

---

## Дорожная карта

- **Фаза 2** — режим `design_report` с визуальным анализом и генерацией обложки
- **Фаза 3** — интерфейс через Telegram-бот
- **Фаза 4** — Flask API для веб-интеграции
